import { State, Events, Style } from "../global.slint";

export component PageHeader inherits Text {
    font-family: "Menlo";
    font-size: 40px;
    color: Style.color.primary;

    x: 0;
    y: 0;
}